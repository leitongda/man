import styled from 'styled-components'
import { Table, Input } from '@arco-design/web-react'

export const StyledTable = styled(Table)`margin-top: 16px;`
export const MonoTextArea = styled(Input.TextArea)`&& { font-family: monospace; }`
