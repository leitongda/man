import styled from 'styled-components'
import { Card, Typography } from '@arco-design/web-react'

const { Paragraph } = Typography

export const StorylineCard = styled(Card)`margin-top: 16px;`
export const BeatDescription = styled(Paragraph)`&& { margin: 8px 0 0; }`
